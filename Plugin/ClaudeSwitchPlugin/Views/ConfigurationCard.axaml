<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:ui="using:FluentAvalonia.UI.Controls"
             xmlns:models="clr-namespace:ClaudeSwitchPlugin.Models"
             mc:Ignorable="d"
             d:DesignWidth="800"
             d:DesignHeight="120"
             x:Class="ClaudeSwitchPlugin.Views.ConfigurationCard">

    <UserControl.Styles>
        <Style Selector="Border.config-card">
            <Setter Property="Background" Value="{DynamicResource CardBackgroundFillColorDefaultBrush}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource CardStrokeColorDefaultBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="8"/>
            <Setter Property="Padding" Value="16"/>
            <Setter Property="Margin" Value="0,0,0,8"/>
        </Style>

        <Style Selector="Border.config-card:pointerover">
            <Setter Property="Background" Value="{DynamicResource CardBackgroundFillColorSecondaryBrush}"/>
        </Style>

        <Style Selector="Border.tag">
            <Setter Property="Background" Value="{DynamicResource AccentFillColorDefaultBrush}"/>
            <Setter Property="CornerRadius" Value="12"/>
            <Setter Property="Padding" Value="8,4"/>
        </Style>

        <Style Selector="Border.tag TextBlock">
            <Setter Property="FontSize" Value="11"/>
            <Setter Property="Foreground" Value="White"/>
        </Style>
    </UserControl.Styles>

    <Border Classes="config-card" Name="CardBorder">
        <Grid RowDefinitions="Auto,Auto" ColumnDefinitions="Auto,*,Auto">

            <!-- 提供商图标 -->
            <Border Grid.Row="0" Grid.Column="0" Grid.RowSpan="2"
                    Width="48" Height="48"
                    VerticalAlignment="Top"
                    Margin="0,0,16,0"
                    CornerRadius="24"
                    Background="{DynamicResource SystemFillColorAttentionBrush}">
                <TextBlock Name="ProviderIcon"
                           FontSize="20"
                           FontWeight="Bold"
                           HorizontalAlignment="Center"
                           VerticalAlignment="Center"
                           Foreground="White"/>
            </Border>

            <!-- 配置信息 -->
            <StackPanel Grid.Row="0" Grid.Column="1" Spacing="4" VerticalAlignment="Center">
                <!-- 配置名称 -->
                <TextBlock Name="ConfigName"
                           FontSize="16"
                           FontWeight="SemiBold"/>

                <!-- 基础 URL -->
                <TextBlock Name="BaseURL"
                           FontSize="12"
                           Opacity="0.6"
                           TextTrimming="CharacterEllipsis"/>
            </StackPanel>

            <!-- 操作按钮 -->
            <StackPanel Grid.Row="0" Grid.Column="2"
                        Orientation="Horizontal"
                        Spacing="8"
                        VerticalAlignment="Center"
                        Margin="16,0,0,0">
                <!-- 应用按钮 -->
                <Button Content="应用"
                        Name="ApplyButton"
                        Padding="16,8"/>
                <!-- 更多操作 -->
                <Button Content="⋯"
                        FontSize="16"
                        FontWeight="Bold"
                        Padding="12,8"
                        Name="MoreButton">
                    <Button.Flyout>
                        <MenuFlyout Placement="BottomEdgeAlignedRight">
                            <MenuItem Header="编辑" Name="EditMenuItem"/>
                            <MenuItem Header="删除" Name="DeleteMenuItem"/>
                        </MenuFlyout>
                    </Button.Flyout>
                </Button>
            </StackPanel>

            <!-- 标签和元数据 -->
            <StackPanel Grid.Row="1" Grid.Column="1" Grid.ColumnSpan="2"
                        Orientation="Horizontal"
                        Spacing="8"
                        Margin="0,8,0,0">

                <!-- 活跃状态标签 -->
                <Border Classes="tag"
                        Background="{DynamicResource SystemFillColorSuccessBrush}"
                        VerticalAlignment="Center"
                        Name="ActiveTag"
                        IsVisible="False">
                    <TextBlock Text="当前应用中"/>
                </Border>

                <!-- API Key -->
                <Border VerticalAlignment="Center"
                        Background="{DynamicResource SubtleFillColorSecondaryBrush}"
                        CornerRadius="4"
                        Padding="6,3">
                    <StackPanel Orientation="Horizontal" Spacing="4">
                        <TextBlock Text="🔒" FontSize="11"/>
                        <TextBlock Name="MaskedApiKey"
                                   FontSize="11"
                                   VerticalAlignment="Center"
                                   FontFamily="Consolas"
                                   Opacity="0.8"/>
                    </StackPanel>
                </Border>

                <!-- 模型名称 -->
                <Border VerticalAlignment="Center"
                        Background="{DynamicResource SubtleFillColorSecondaryBrush}"
                        CornerRadius="4"
                        Padding="6,3"
                        Name="ModelBorder"
                        IsVisible="False">
                    <StackPanel Orientation="Horizontal" Spacing="4">
                        <TextBlock Text="⚙" FontSize="11"/>
                        <TextBlock Name="ModelName"
                                   VerticalAlignment="Center"
                                   FontSize="11"
                                   Opacity="0.8"/>
                    </StackPanel>
                </Border>

                <!-- 最后使用时间 -->
                <TextBlock VerticalAlignment="Center"
                           FontSize="11"
                           Opacity="0.5"
                           Name="LastUsedText"/>
            </StackPanel>
        </Grid>
    </Border>
</UserControl>