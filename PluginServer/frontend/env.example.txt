# =====================================================
# MicroDock 插件服务器 - 前端配置文件
# =====================================================
# 使用方法: 复制此文件为 .env 或 .env.local 并根据需要修改
# 注意: Vite 环境变量必须以 VITE_ 前缀开头才能被识别

# 前端开发服务器监听地址 (0.0.0.0 允许外部访问, 127.0.0.1 仅本地)
VITE_HOST=0.0.0.0
# 前端开发服务器端口 (默认 3000)
VITE_PORT=3000

# 后端 API 地址 (用于代理配置)
# 开发环境通常是 http://localhost:8000
VITE_API_URL=http://localhost:8000